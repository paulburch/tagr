## population size with bootstrapped uncertainty using multiple releases


## this function now takes a vector of tags, catch and recaptures
check_mrelease_inputs <- function(tags, catch, recaps){
  ## define the check variable
  check <- TRUE
  ## add checks
  ## return check
  check
}
